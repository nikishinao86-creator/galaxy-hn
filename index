<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Hunter Network</title>
  <script src="https://telegram.org/js/telegram-web-app.js"></script>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: #000;
      color: #fff;
      text-align: center;
      padding: 60px 20px;
      margin: 0;
    }
    .galaxy {
      margin: 40px auto;
      width: 200px;
      height: 200px;
      background: radial-gradient(circle, #3a86ff, #000);
      border-radius: 50%;
      box-shadow: 0 0 20px #3a86ff;
    }
  </style>
</head>
<body>
  <h1>🌌 Hunter Network</h1>
  <div class="galaxy"></div>
  <p id="info">Загрузка...</p>

  <script>
    const tg = window.Telegram.WebApp;
    tg.ready();
    tg.expand();

    const user = tg.initDataUnsafe.user;
    const startParam = tg.initDataUnsafe.start_param;

    if (user) {
      document.getElementById("info").textContent = `Добро пожаловать, ${user.first_name}!`;
    } else {
      document.getElementById("info").textContent = `Запущено в Telegram`;
    }

    // Отправим реферальный параметр на сервер (позже)
    if (startParam) {
      console.log("Присоединился по коду:", startParam);
    }
  </script>
</body>
</html>
